<div id="gigs-new">
  <h3 class="header">Add Event</h3>

  <%= render "form", gig: @gig %>
</div>

<% if @recommendation %>
  <%= render "create_gig_modal", recommendation: @recommendation, conflicts: @conflicts %>

  <script type="text/javascript">
    $("#create-gig-modal").modal("show");

    $("#confirm-button").click(function() {
      $("#create-gig-modal").modal("hide");
      $("#double_confirmation").val(true);
      $(".new_gig").submit();
    });
  </script>
<% end %>
